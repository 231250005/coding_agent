# 工具能力总览（16 个，五类）

> 所有工具 = JSON schema 定义 + 本地实现，统一走 ToolRegistry 注册。
> 路径一律相对工作区；输出带截断（头部/尾部策略）+ 截断元数据（truncated/total_chars）。
> 执行走线程池（不阻塞事件循环）；不可逆命令黑名单拦截。

---

## 一、文件操作类（3 个）

### 1. `write_file` — 写入文件
- **能力**：新建或覆盖写入一个文件（UTF-8），自动创建缺失的父目录
- **参数**：`path`（相对工作区路径）、`content`（完整文件内容）
- **场景**：生成新代码文件、全量重写文件
- **权限联动**：L1 → 软修改（pending 等待确认）；L2/L3 → 直接写入并记录变更（可撤销）；同文件多次修改自动合并（old=最早、new=最新）
- **安全**：路径防穿越（safe_join 强制工作区内）

### 2. `read_file` — 读取文件
- **能力**：读取文件内容（UTF-8），**带行号输出**（便于模型定位报错行）；支持 `start_line`/`end_line` 分段读取大文件
- **参数**：`path`、`start_line`（可选）、`end_line`（可选）
- **场景**：查看代码、定位报错行、读取项目规范
- **权限联动**：L1 下读取 pending 变更的"虚拟内容"（待确认的新内容）
- **边界**：超过 500 行/8000 字符自动截断（头部保留），附带截断元数据

### 3. `edit_file` — 精确修改
- **能力**：把 `old_string` 替换为 `new_string`（可多行），实现**局部修改**，避免整文件重写
- **参数**：`path`、`old_string`、`new_string`、`replace_all`（可选）
- **匹配策略**：① 精确子串匹配（唯一性校验）② 整行容错匹配（行尾空白/\r\n 差异）
- **可行动错误**：匹配不唯一 → 提示出现次数与行号；找不到 → 建议先 read_file 再看
- **权限联动**：同 write_file（L1 软修改/可撤销/合并）
- **场景**：改一行常量、改一个函数、修复 bug

---

## 二、代码库探索类（4 个）

### 4. `list_dir` — 列出目录
- **能力**：列出工作区目录内容（文件/子目录标记）
- **参数**：`path`（可选，默认根目录）
- **场景**：任务开始时了解工作区结构、确认文件已生成

### 5. `grep` — 内容搜索
- **能力**：正则搜索文件内容，返回 **文件:行号:匹配行**
- **参数**：`pattern`（必填）、`path`（文件或目录）、`file_glob`（文件类型过滤）
- **场景**：定位函数调用处、搜索报错关键字、找 TODO
- **边界**：自动跳过 .git/缓存/依赖目录；结果上限 50 条

### 6. `glob` — 文件名匹配
- **能力**：按模式查找文件路径（支持 `**` 递归）
- **参数**：`pattern`（如 `**/*.py`、`test_*.py`）
- **场景**：找所有测试文件、统计项目文件分布
- **与 grep 的区别**：grep 搜内容，glob 按文件名匹配路径

### 7. `find_symbols` — 符号定位（AST）
- **能力**：用 Python AST 解析文件，返回**函数/类定义位置**（文件名:行号 + 签名）
- **参数**：`path`（必填）、`name`（可选过滤）
- **场景**：修改某函数前精确定位；比 grep "def xxx" 零误报（语法树级）
- **示例输出**：`def _clear_lines @ tetris.py:178  (self)`

---

## 三、执行与测试类（4 个）

### 8. `run_command` — 执行命令
- **能力**：在 Windows cmd 中执行命令（shell=True），工作区为 cwd
- **参数**：`command`（必填）、`timeout`（默认 60s）、`background`（后台启动）
- **场景**：安装依赖（pip install）、运行程序（用户明确要求时）、查看环境
- **安全**：**不可逆命令黑名单**（递归/通配符删除、git clean、磁盘级操作一律拒绝，任何权限模式不可绕过）；超时杀进程树；输出**尾部截断**（保留末尾错误信息）+ 截断元数据
- **background=true**：后台启动（独立进程组、不等待不终止），GUI 窗口常驻可玩
- **注意**：测试执行请用 run_tests；语法检查/评审请用 code_review

### 9. `run_python` — Python 片段执行
- **能力**：直接执行 Python 代码字符串（不走 shell，无转义/注入风险）
- **参数**：`code`（必填）、`timeout`（默认 30s）
- **场景**：验证算法片段、计算结果、测试小函数
- **输出**：尾部截断 + 元数据

### 10. `run_tests` — 运行测试
- **能力**：运行测试并返回**结构化结果**（通过/失败/错误数 + 失败用例明细）
- **参数**：`path`（指定测试文件/目录，**支持用户已有测试**）、`keyword`（-k 过滤）、`timeout`（默认 120s）
- **场景**：测试通过后验证、失败后定位失败用例
- **输出示例**：`测试结果：8 通过 / 2 失败 / 0 错误` + `FAILED test_remove.py::test_remove_by_id - AssertionError: ...`
- **注意**：自动禁用 pytest 缓存（不生成 .pytest_cache）

### 11. `generate_test` — 生成测试
- **能力**：为指定代码文件**自动生成 pytest 测试样例**（覆盖正常/边界/异常路径），产出 `test_<名>.py`
- **参数**：`path`（目标代码文件）
- **实现**：内部调 LLM（分析代码 → 生成测试 → 写文件），与 run_tests 配合"生产→执行"闭环
- **防护**：测试文件已存在时拒绝覆盖（防止误删用户手写测试）

---

## 四、Git 操作类（4 个）

### 12. `git_status` — 查看状态
- **能力**：简化版 git status --short，返回改动文件列表
- **场景**：任务开始前了解仓库状态、修改后确认改动

### 13. `git_diff` — 查看差异
- **能力**：返回未提交改动的 +/- 行文本；`path` 指定文件时只看该文件
- **场景**：评审前了解改了什么、确认改动正确

### 14. `git_commit` — 提交
- **能力**：git add -A + commit，返回 commit 哈希
- **参数**：`message`（必填）
- **场景**：L3 权限任务完成时自动提交（Agent.finalize_commit）
- **防护**：非 git 仓库/无可提交改动 → 友好错误

### 15. `git_log` — 查看历史
- **能力**：git log --oneline，返回最近 N 次提交（哈希+信息）
- **参数**：`limit`（默认 20，最大 100）
- **场景**：了解仓库演进、回溯上次提交

---

## 五、代码质量类（1 个）

### 16. `code_review` — 代码评审
- **能力**：**内建编译器级语法检查（ast.parse）+ 评审者视角审查**（逻辑/需求/质量/安全），输出问题清单与修复建议
- **参数**：`path`（目标文件，可选——省略时自动收集 git diff 或最近修改文件）、`context`（任务需求，供需求符合性检查）
- **审查维度**：语法正确性（机械检查）→ 功能正确性 → 需求符合性 → 代码质量 → 安全性
- **评审标准**：能正常运行即达标（不追求完美）；仅语法错误/崩溃/核心功能错误列 [严重]
- **输出**：`✅ 评审通过` 或 `[严重/一般/建议] 位置: 问题 → 修复建议`
- **场景**：每次写完/修改代码后的必做评审；实现过程中自查

---

## 工具间协作（典型闭环）

```
写代码:  write_file/edit_file → code_review（评审修复 ≤2 轮）
测试:    generate_test（生成） → run_tests（运行，≤2 轮）
提交:    L3 权限下任务完成自动 git_commit
探索:    list_dir/glob（找文件）→ grep/find_symbols（定位）→ read_file（读）
```
