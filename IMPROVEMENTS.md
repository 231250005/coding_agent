# 待改进点清单（持续更新）

> 只记录**尚未完成**的改进点；已完成项已移除（见 git 历史 commit 记录）。
> 时间：2026-08-31 ｜ 截止：2026-09-02 24:00

---

## 一、待改进点

### A. 安全类

| # | 改进点 | 问题 | 方案 | 成本 | 优先级 |
|---|---|---|---|---|---|
| A2 | **会话工作区冲突提示** | 两个会话指向同一 workspace 会互相覆盖修改 | 创建会话时查相同 workspace 的会话，返回 `warning` 字段（不阻断） | 5 行 | 🟡 中 |
| A3 | **工具参数运行时校验** | 模型传错参数类型时靠 str() 强转 + 异常兜底，错误信息模糊 | 工具入口做必填/类型检查，失败回结构化错误 | 30 行 | 🟡 中 |

### B. 上下文管理类

| # | 改进点 | 问题 | 方案 | 成本 | 优先级 |
|---|---|---|---|---|---|
| B2 | **截断元数据** | 模型只看到截断提示文本，无法结构化判断输出被截了 | tool_result 加 `truncated: true` + `total_chars` 字段 | 10 行 | 🟡 中 |
| B3 | **AGENTS.md 支持** | 项目级指令（如"用 pytest 不用 unittest"）无法注入 | 系统提示词追加 AGENTS.md 内容（存在则读取） | 15 行 | 🟡 中 |
| B4 | **usage 锚定估算** | token 估算（CJK 1 字≈1 token）与实际偏差，压缩阈值判断不准 | assistant 消息保存真实 `usage.prompt_tokens`，估算时用它做锚点 | 20 行 | 🟡 中 |
| B5 | **安全切割点** | 压缩可能劈开完整轮次/工具结果对（模型看到"摘要→半截对话"） | 切割点选在消息边界，工具结果跟随其 assistant 消息 | 40 行 | 🟡 中 |

### C. 架构类（建议不做，面试讲取舍）

| # | 方向 | 为什么不做 |
|---|---|---|
| C1 | 五阶段 Loop 生命周期（prepare_context/stream_model/execute_tools/commit） | 架构更漂亮，但当前 ReAct 已内聚可用；半天成本换"面试好看"不划算 |
| C2 | 分支/树状会话（checkout/rollback 不删历史） | 演示用不上；文件级撤销（revert）比对话级回滚更精准 |
| C3 | 事件溯源 / 崩溃恢复 | 单用户本地任务，成本 > 收益；已有"预算护栏 + 迭代上限 + 异常兜底"三层防护 |
| C4 | 并行工具执行（同轮多 tool_calls） | 串行稳健可辩护，演示无感 |
| C5 | token 级流式（打字机效果） | 前端加分但非必需，时间紧 |

### D. 前端/产品类（Vue 实现时）

| # | 点 | 说明 |
|---|---|---|
| D1 | **确认卡片交互** | request_confirmation 到达时：对话流插入卡片、**输入框禁用**、自动滚动到卡片、"等待确认"状态；**每次事件都渲染新卡片（勿按 change_id 去重——合并逻辑下同文件多次修改会复用 change_id）** |
| D2 | **变更面板** | diff 对比视图；撤销 409 冲突时弹窗提示 + 刷新列表；"保存全部"按钮（POST confirm-all） |
| D3 | 权限选择器 | 每轮发消息可选 L1/L2/L3 |
| D4 | 工作区选择器 | 目录树（GET /api/fs/dirs）或原生对话框 + resolve（GET /api/fs/resolve） |
| D5 | 用量条 | SSE usage 事件 → 上下文 token 进度 + 调用次数 |

### E. 测试类

| # | 点 | 说明 |
|---|---|---|
| E1 | 循环场景 mock 测试增强 | FakeProvider 风格：多轮工具调用、L1 确认、压缩触发等更多单元场景 |

---

## 二、优先级与时间预算

```
今天（8/31）：🟡 有余力做 B3（AGENTS.md，15 行）或 B2（截断元数据，10 行）
明天（9/1）：留时间给前端联调 + 视频 + README + 打包提交
面试话术（不做但能讲）：C1-C5、E1 的取舍
```

**核心建议**：待改进点均为 🟡 可选；**前端 + 视频是当前最紧**。

---

## 三、Skill 发展方向分析

**Skill 的本质**：可复用的**提示词级能力封装**（Claude Code 的 SKILL.md / pi 的 skills）——按需加载的技能描述 + 指令文件，不占常驻上下文。

### 当前状态

```
工具层：ToolRegistry 注册表（能力注册模式）✅ —— 加工具 = 加类 + 注册一行
提示词层：静态 prompts.py（所有工具规则硬编码）❌ —— 加工具要手改提示词
```

### 升级路径（三步）

```
第一步（提示词数据驱动）：工具 rules 从 prompts.py 拆到每工具一个 snippet，
     buildSystemPrompt 动态拼装（可用工具列表 + 每工具一行描述）—— 1-2 小时
第二步（Skill 注册表）：像 ToolRegistry 一样建 SkillRegistry——
     每个 skill = {name, description, content}
     模型可见 description（进系统提示词），调用时读 content —— 半天
第三步（SKILL.md 文件体系）：每 skill 一个 md 文件，按需读取 —— 半天+
```

### 值不值得做

- **演示层面**：skill 是"架构方向"，无独立演示效果
- **面试层面**：一句话即可讲："我的工具注册表已经是能力注册模式，提示词层可以同样做——把工具规则拆成 snippet、再升级为 SKILL.md 文件体系，就是 Claude Code 的 skills 机制"
- **结论**：不做实现，面试讲"我知道升级路径，工具注册表是它的地基"

---

## 四、已完成项（git 历史可查，此处不展开）

✅ 不可逆命令黑名单（A1）✅ 命令输出尾部截断（B1）✅ 三级权限系统
✅ 撤销冲突检测 ✅ 同文件修改合并 ✅ 保存全部 ✅ 删除会话级联清理
✅ 多轮记忆（持久化摘要）✅ 上下文压缩 ✅ 评审/测试轮次控制
✅ 预算护栏 + 重试分类 ✅ finish_reason 截断处理 ✅ 事件循环修复
✅ 缓存目录治理 ✅ 运行行为（background 启动）✅ SSE 订阅者治理
✅ 接口 E2E 测试（api_e2e_check）
