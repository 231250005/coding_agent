# 改进点清单（已全部完成）

> 所有待改进点已实现（A1/A3/B1-B5）。本文档保留：已决定不做的取舍、Skill 发展方向、已完成项汇总。
> 时间：2026-08-31 ｜ 截止：2026-09-02 24:00

---

## 一、待改进点：已清空 ✅

| 项 | 状态 |
|---|---|
| A1 不可逆命令黑名单 | ✅ 已完成 |
| A3 工具参数运行时校验 | ✅ 已完成 |
| B1 命令输出尾部截断 | ✅ 已完成 |
| B2 截断元数据 | ✅ 已完成 |
| B3 AGENTS.md 分级注入（短文件全文/超长 LLM 摘要） | ✅ 已完成 |
| B4 usage 锚定估算 | ✅ 已完成 |
| B5 安全切割点 | ✅ 已完成 |

## 二、已决定不做（面试讲取舍，不实现）

| 项 | 决定理由 |
|---|---|
| 分支/树状会话 | 文件级撤销已覆盖"改错了"；分支解决"对话探索"，演示用不上 |
| 事件溯源/崩溃恢复 | 单用户本地，成本 > 收益；已有"预算护栏 + 迭代上限 + 异常兜底"三层防护 |
| 并行工具执行 | 串行稳健（每步基于真实结果）、无竞态；并行是性能取向，演示无感 |
| token 级流式后端 | 前端已实现打字机效果（逐字渲染），无需后端 token 流 |
| 五阶段 Loop 生命周期 | 架构更漂亮，但当前 ReAct 已内聚可用；成本换"面试好看"不划算 |
| 会话工作区冲突提示 | 写入乐观（实时读最新）+ 撤销冲突检测已覆盖，提示多余 |

## 三、Skill 发展方向分析

**Skill 的本质**：可复用的**提示词级能力封装**（Claude Code 的 SKILL.md / pi 的 skills）——按需加载的技能描述 + 指令文件，不占常驻上下文。

### 当前状态

```
工具层：ToolRegistry 注册表（能力注册模式）✅ —— 加工具 = 加类 + 注册一行
提示词层：静态 prompts.py（所有工具规则硬编码）❌ —— 加工具要手改提示词
```

### 升级路径（三步）

```
第一步（提示词数据驱动）：工具 rules 从 prompts.py 拆到每工具一个 snippet，
     buildSystemPrompt 动态拼装（可用工具列表 + 每工具一行描述）—— 1-2 小时
第二步（Skill 注册表）：像 ToolRegistry 一样建 SkillRegistry——
     每个 skill = {name, description, content}
     模型可见 description（进系统提示词），调用时读 content —— 半天
第三步（SKILL.md 文件体系）：每 skill 一个 md 文件，按需读取 —— 半天+
```

### 值不值得做

- **演示层面**：skill 是"架构方向"，无独立演示效果
- **面试层面**：一句话即可讲："我的工具注册表已经是能力注册模式，提示词层可以同样做——把工具规则拆成 snippet、再升级为 SKILL.md 文件体系，就是 Claude Code 的 skills 机制"
- **结论**：不做实现，面试讲"我知道升级路径，工具注册表是它的地基"

## 四、已完成项汇总

### Agent 核心
- ✅ ReAct 单框架 + 规范化工具调用（16 工具：文件/探索/执行/测试/git/评审）
- ✅ 三级权限系统（L1 软修改确认 / L2 可撤销 / L3 自动提交）
- ✅ 评审/测试轮次硬控制（各最多 2 轮）
- ✅ LLM 预算护栏 + 重试分类（400 不重试 / 429·5xx·连接重试）
- ✅ finish_reason="length" 截断处理 + 参数 JSON 解析失败明确报错
- ✅ 事件循环修复（工具执行放线程池，任务运行期间 HTTP 保持响应）
- ✅ 不可逆命令黑名单（A1）+ 命令输出尾部截断（B1）
- ✅ 工具参数运行时校验（A3）
- ✅ 缓存目录源头治理 + 任务完成后不主动运行验证（background 启动）

### 上下文管理
- ✅ 多轮记忆（持久化会话摘要，零硬截断）
- ✅ 上下文压缩（token 估算 + 两级：裁剪工具结果/LLM 摘要）
- ✅ 截断元数据（B2：truncated/total_chars）
- ✅ AGENTS.md 分级注入（B3：短文件全文/超长 LLM 摘要）
- ✅ usage 锚定估算（B4）
- ✅ 安全切割点（B5：不劈开轮次/工具对）

### 权限与文件变更
- ✅ 同文件多次修改合并（old=最早 / new=最新）
- ✅ 撤销冲突检测（409 + conflict 识别信息）
- ✅ 保存全部（confirm-all）+ 删除会话级联清理
- ✅ SSE 订阅者治理（无订阅者丢弃事件，防泄漏）

### 后端与前端
- ✅ 12 接口 + fs 浏览/解析 + SSE 事件流 + MySQL 3 表（自动建库建表同步）
- ✅ 接口 E2E 测试（api_e2e_check.py）+ 各层 mock 测试
- ✅ 前端全部完成（确认卡片/变更面板/权限选择器/工作区选择器/用量条）
